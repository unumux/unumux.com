// Main JS should go here!
// Include scripts using Browserify by doing:
// var $ = require("jquery");


$('.carousel').slick({
    autoplay: true,
    autoplaySpeed: 4000,
    arrows: false,
    dots: true,
    mobileFirst: true,
    swipeToSlide: true
});